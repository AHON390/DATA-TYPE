import calendar

month_number = int(input("Enter a month number (1-12): "))

if 1 <= month_number <= 12:
    print("Month name:", calendar.month_name[month_number])
else:
    print("Invalid month number. Please enter a number between 1 and 12.")
